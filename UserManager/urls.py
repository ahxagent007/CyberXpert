from django.urls import path
from .views import *

urlpatterns=[
    # path('login', Login, name='Login'),
    # path('logout', Logout, name='Logout'),
    # path('view/list', UserList, name='UserList'),
    # path('add', UserAdd, name='UserAdd')


]